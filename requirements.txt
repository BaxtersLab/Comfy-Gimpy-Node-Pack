# Runtime dependencies for Comfy Gimpy Studio
# Core
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
Pillow>=9.0.0

# HTTP / WebSocket (ComfyUI client-side bridge)
requests>=2.28.0
websockets>=11.0.0
aiohttp>=3.8.0
aiofiles>=23.0.0

# Web interface (standalone Flask server)
flask>=2.3.0
flask-cors>=4.0.0

# System monitoring
psutil>=5.9.0

# Optional: distributed caching (needed for performance optimizer)
# redis>=4.5.0
